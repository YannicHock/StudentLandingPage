import React from 'react';
import CollapsibleSubtitle from '../CollapsibleSubtitle';

const ModulDatabase: React.FC = () => {
    return (
        <CollapsibleSubtitle subtitle={"Moduldatenbank"}>
            <p>Moduldatenbank</p>
        </CollapsibleSubtitle>
    );
};

export default ModulDatabase;